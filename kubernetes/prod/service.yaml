apiVersion: v1
kind: Service
metadata:
  name: java-app
  namespace: prod
spec:
  selector:
    app: java-app
  type: LoadBalancer
  ports:
    - port: 80        
      targetPort: 80  
      protocol: TCP
